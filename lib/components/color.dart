import 'package:flutter/material.dart';

class safecolor extends StatelessWidget {
  const safecolor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}